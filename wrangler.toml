name = "pimx-wide"
compatibility_date = "2024-12-01"

# D1 Database binding — must match the binding name configured in
# Cloudflare Dashboard → Pages → Your Project → Settings → Functions → D1 bindings
[[d1_databases]]
binding = "PIMX_VISITS"
database_name = "pimx-visits"
database_id = "d588d3ed-0211-4de2-ae9d-849064ea403e"  # Replace after creating D1 in Cloudflare Dashboard
