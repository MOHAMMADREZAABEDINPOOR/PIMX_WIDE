name = "pimx-wide"
compatibility_date = "2024-12-01"

# D1 Database binding — must match the binding name configured in
# Cloudflare Dashboard → Pages → Your Project → Settings → Functions → D1 bindings
[[d1_databases]]
binding = "PIMX_VISITS"
database_name = "pimxwide-visits"
database_id = "88747c7d-f5f3-443a-8500-34c246d67d36"  # Replace after creating D1 in Cloudflare Dashboard
